# D1W17

Progetto diviso in `backend/` (Spring Boot, completo) e `frontend/` (da costruire).

Prima di lavorare sul frontend leggi `frontend/RESUME.md`: contiene API, limiti del backend e i passi da seguire.
